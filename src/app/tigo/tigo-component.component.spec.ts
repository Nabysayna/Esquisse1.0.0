import { async, ComponentFixture, TestBed } from '@angular/core/testing';

/*import { TigoComponentComponent } from './tigo-component.component';

describe('TigoComponentComponent', () => {
  let component: TigoComponentComponent;
  let fixture: ComponentFixture<TigoComponentComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TigoComponentComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TigoComponentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
